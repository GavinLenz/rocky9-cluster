---
- name: Stage iPXE binaries
  ansible.builtin.copy:
    src: "{{ item.src }}"
    dest: "{{ pxe_settings.tftp_root }}/{{ item.dest }}"
    remote_src: true
    owner: root
    group: root
    mode: '0644'
  loop:
    - src: "{{ pxe_settings.uefi_src }}"
      dest: "{{ pxe_settings.uefi_name }}"
  when: not ansible_check_mode
